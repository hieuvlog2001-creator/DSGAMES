{
  "name": "dsgames-catalog",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute dsgames --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
